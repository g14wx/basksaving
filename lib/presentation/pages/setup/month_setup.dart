import 'package:flutter/material.dart';

class MonthSetup extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(''),
      ),
      body: Column(
        children: [
          Text('Month')
        ],
      ),
      backgroundColor: Colors.white,
    );
  }
}
